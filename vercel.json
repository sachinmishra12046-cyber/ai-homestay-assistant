{
  "buildCommand": "prisma generate && next build",
  "devCommand": "next dev",
  "installCommand": "npm install",
  "framework": "nextjs",
  "regions": ["sin1"],
  "env": {
    "NEXTAUTH_URL": {
      "description": "The URL of your Next.js app (auto-set by Vercel)",
      "value": ""
    },
    "NEXTAUTH_SECRET": {
      "description": "Secret for NextAuth session encryption",
      "value": "",
      "generator": "secret"
    }
  }
}
